
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}


<div id="wrapper">
    <div id="content-wrapper">
        <div class="container-fluid">
            <div class="row">
                <div class="col-xl-4 col-sm-4 mb-3">
                    <div class="card text-white bg-primary o-hidden h-100">
                        <div class="card-body">
                            <div class="card-body-icon">
                                <i class="fas fa-fw fa-comments"></i>
                            </div>
                            <div class="mr-5">100 New Students!</div>
                        </div>
                        <a class="card-footer text-white clearfix small z-1" href="#" onclick="addView(1)">
                            <span class="float-left">Add a new student</span>
                            <span class="float-right">
                                <i class="fas fa-angle-right"></i>
                            </span>
                        </a>
                    </div>
                </div>
                <div class="col-xl-4 col-sm-4 mb-3">
                    <div class="card text-white bg-warning o-hidden h-100">
                        <div class="card-body">
                            <div class="card-body-icon">
                                <i class="fas fa-fw fa-list"></i>
                            </div>
                            <div class="mr-5">50 New Teachers!</div>
                        </div>
                        <a class="card-footer text-white clearfix small z-1" href="#" onclick="">
                            <span class="float-left">Add a new teacher</span>
                            <span class="float-right">
                                <i class="fas fa-angle-right"></i>
                            </span>
                        </a>
                    </div>
                </div>
                <div class="col-xl-4 col-sm-4 mb-3">
                    <div class="card text-white bg-success o-hidden h-100">
                        <div class="card-body">
                            <div class="card-body-icon">
                                <i class="fas fa-fw fa-shopping-cart"></i>
                            </div>
                            <div class="mr-5">2 New Administrators!</div>
                        </div>
                        <a class="card-footer text-white clearfix small z-1" href="#" onclick="">
                            <span class="float-left">Add a new admin</span>
                            <span class="float-right">
                                <i class="fas fa-angle-right"></i>
                            </span>
                        </a>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>


<!--Popup model edit and view-->
<div class="modal fade" id="popupModal">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body site-theme-model" id="popupModalBodyDiv">
                @*popupPV*@
            </div>
        </div>
    </div>
</div>

<script src="~/Areas/Admin/scripts/jquery.min.js"></script>
<script>

    $(document).ready(function () {

        var resultCode = @Html.Raw(Json.Encode(TempData["ResultCode"]));
        var resultMessage = @Html.Raw(Json.Encode(TempData["ResultMessage"]));

        if (resultCode == 1) {
            toastr.success(resultMessage.toString());
        }

        if (resultCode == 2) {
            toastr.error(resultMessage.toString());
        }
    });

    var addView = function (type) {

        var url = '';
        if (type==1) {
            url = '@Url.Action("AddStudent", "ManagePeople")';
        }
        if (type==2) {
            url = '@Url.Action("AddTeacher", "ManagePeople")';
        }
        if (type==3) {
            url = '@Url.Action("AddAdmin", "ManagePeople")';
        }

        $("#popupModalBodyDiv").load(url, function () {
            $("#popupModal").modal('show');
        })

    }
</script>